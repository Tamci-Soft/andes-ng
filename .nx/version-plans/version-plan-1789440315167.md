---
ui: minor
---

Add htmlType (as 'type', defaulting to 'button' - never the native submit default), target (for href), and a real ghost display mode (transparent, tinted to the variant's own color - distinct from the existing ghost variant), matching Ant Design's Button.
