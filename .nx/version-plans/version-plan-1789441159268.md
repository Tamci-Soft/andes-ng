---
ui: minor
---

Add dashed variant, a customizable loadingIcon (TemplateRef, falls back to the default spinner), and auto-sizing for any projected SVG icon (1em, scales with each size class's font-size).
