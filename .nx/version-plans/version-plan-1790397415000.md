---
tokens: minor
---

Adopt Geist as the default typeface: add an optional `fonts.css` entry point that bundles Geist and Geist Mono, point `--andes-font-family` at Geist (system fonts as fallback) and add `--andes-font-family-mono`, a font-size / line-height scale (`xs` to `2xl`) and regular/semibold/bold weights.
