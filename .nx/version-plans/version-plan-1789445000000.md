---
primitives: minor
---

Add a shared overlay behavior primitive (`AndesOverlayPrimitive` plus trigger/content/close directives) that Dialog, Drawer/Sheet, Popover, Tooltip and Dropdown Menu build on: portal rendering, anchored/centered/edge positioning with collision handling, optional focus trap and focus restoration, independently toggleable Escape and click-outside dismissal, scroll lock, and z-index/scrim wired to the `--andes-z-index-*` and `--andes-color-overlay` tokens.
