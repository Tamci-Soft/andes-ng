# @andes-ng/testing

Stable test helpers for applications that consume Andes NG. Helpers will target Andes public selectors and semantics; they will never expose Spartan Brain implementation details.

**The package is reserved and exposes no API yet.** Harnesses and query helpers will be added alongside the components they support, so that a helper is never published before the contract it queries.
